import { DSPage } from '../../layout/DSPage';
import { ScrollAreaSection } from '../../sections/ScrollAreaSection';

export function ScrollAreaPage() {
  return (
    <DSPage title="Scroll Area">
      <ScrollAreaSection />
    </DSPage>
  );
}
