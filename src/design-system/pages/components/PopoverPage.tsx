import { DSPage } from '../../layout/DSPage';
import { PopoverSection } from '../../sections/PopoverSection';

export function PopoverPage() {
  return (
    <DSPage title="Popover">
      <PopoverSection />
    </DSPage>
  );
}
