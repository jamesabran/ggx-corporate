import { DSPage } from '../../layout/DSPage';
import { PaginationSection } from '../../sections/PaginationSection';

export function PaginationPage() {
  return (
    <DSPage title="Pagination">
      <PaginationSection />
    </DSPage>
  );
}
