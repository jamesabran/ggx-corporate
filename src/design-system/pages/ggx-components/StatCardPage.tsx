import { DSPage } from '../../layout/DSPage';
import { StatCardSection } from '../../sections/StatCardSection';

export function StatCardPage() {
  return (
    <DSPage title="Stat Card">
      <StatCardSection />
    </DSPage>
  );
}
